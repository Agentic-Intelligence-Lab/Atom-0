# Baseline

Training config: `pi05_no_ki_libero`.

The baseline disables KI, MEM, and DCC. Use it as a comparison only when its
initialization, batch size, data split, training steps, and evaluator match the
variant being tested.
